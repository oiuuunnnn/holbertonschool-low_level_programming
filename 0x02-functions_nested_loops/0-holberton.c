#include "_putchar.c"

/**
*main - entry point
*
*Return: string
*/

int main(void)
{
int st(9) = "Holberton";
int ln;

	for (int x = 0, x <= ln, x++)
	ln = strlen(st);
	_putchar(st);

_putchar('\n');
return (0);
}
